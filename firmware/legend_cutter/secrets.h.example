// Copy this file to `secrets.h` and fill in your credentials.
// IMPORTANT: Do not commit `secrets.h` (it is in .gitignore).

#pragma once

// WiFi Access Point credentials — the ESP32 creates this network
#define AP_SSID      "LegendCutter"
#define AP_PASSWORD  "coastguard"

// OTA update password (ArduinoOTA)
#define OTA_PASSWORD "cutter2026"
